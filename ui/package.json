{
  "name": "agentsshop-ui",
  "private": true,
  "workspaces": [
    "web",
    "desktop",
    "shared"
  ],
  "scripts": {
    "dev:web": "npm -w web run dev",
    "dev:desktop": "npm -w desktop run dev",
    "build:web": "npm -w web run build",
    "build:desktop": "npm -w desktop run build",
    "lint:web": "npm -w web run lint",
    "lint:desktop": "npm -w desktop run lint"
  }
}

