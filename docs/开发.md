
## 模型

### 项目结构
    本项目使用agentscope+agentscoperuntime+React+Electron+Phaser.js + vite，已激活虚拟环境venv并安装了agentscope和agentscoperuntime，
    任务1，生成一个index主页面，顶部有信息栏，左侧列表显示各个agent，右侧是对话信息框，从上往下是agent名称，状态（占位），信息框，对话输入以及发送。
    任务2，用agentscope创建一个agent，名字为sera中文为塞瑞，考虑到是多智能体请务必做好拓展。
    任务3，